## Rules

- Use pnpm for package management. Always run tests before committing.
- When writing React components, prefer functional components with hooks. Use TypeScript generics for reusable utilities.
- Ensure all functions have return types. Make sure to handle errors.

## Examples

```typescript
// This is an example, not a rule
const x = 1;
```

This project uses React for the frontend.
